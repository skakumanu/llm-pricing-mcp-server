{
  "name": "llm-pricing-mcp-server",
  "version": "1.7.0",
  "description": "LLM Pricing MCP Server for Azure App Service - Supporting 12 major LLM providers with geolocation and blue-green deployment",
  "main": "src/main.py",
  "scripts": {
    "start": "python -m uvicorn src.main:app --host 0.0.0.0 --port 8000"
  }
}